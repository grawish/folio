# Release completion audit — 26 September 2026

**Overall goal remains active.** The chat-first amendment is implemented and verified on the development Mac, but the Apple silicon macOS release still has open production requirements. A successful local chat build does not prove that release complete.

This audit uses the current implementation, actual native tests, package artifacts and documented limits. It preserves the original requirements. Items marked partial or open remain required for the Mac release. The user has explicitly removed Windows, Linux, Intel Mac and cross-platform runtime equivalence from this goal.

| Plan area | Current evidence | Remaining work / evidence required |
| --- | --- | --- |
| Desktop targets and feasibility (RM-001–003) | `scripts/runtime-config.mjs` pins inputs. Native Apple silicon compiler, isolation and desktop tests pass. Windows/Linux compilation fails closed. | Apple silicon macOS-version/clean-machine evidence, sandbox review and release support decision. |
| App shell and IPC (RM-004) | Electron, typed preload, sandboxed renderer, constrained IPC, local assets, packaged app. | Independent boundary review and advertised-platform acceptance. |
| Project persistence (RM-005) | Journaled source/asset/manifest/history saves, rollback and real process-kill recovery, outside-file watching/review with preserved buffers and source copies, conflicts at save time, stable IDs, Save As, per-project conversation/history. Guided interrupted-save selection now retains source/conversation drafts and resumes durable choices; unit/native tests are recorded in `SAVE_RECOVERY_IMPLEMENTATION.md`. See also `EXTERNAL_CHANGES.md`. | Project-schema migration interruption coverage, target-platform/network-filesystem/power-loss durability validation. Compiler-change interruption is separately covered under RM-007. |
| Editing (RM-006) | CodeMirror highlighting, completions, snippets, search, per-file undo across tabs/theme/rename/Save As; recoverable source rename/removal/restore with journaled disk deletion and retained copies; Code remains secondary to Chat. Resizable panes and optional journaled autosave with conflict/error pauses are implemented; see `FILE_MANAGEMENT.md` and `WORKSPACE_PREFERENCES.md`. | Broader accessibility/target-platform acceptance. |
| Compilation (RM-007) | Immutable snapshots, bounded subprocess output/time, cancellation, stale-result protection and exact per-project runtime/bundle identities retained in saves/recovery/history/ZIP; explicit backed-up compiler comparison with real PDFs, stale-input checks, forced-interruption/reload/close-during-Apply coverage and final packaged qualification. See `COMPILER_MIGRATION.md`. | Hard memory/disk limits, broader end-to-end/device stop-latency acceptance, bounded historical storage policy and broader Mac acceptance. Nine compiler-group cancellation/timeout measurements are now recorded in `PERFORMANCE_IMPROVEMENT_PLAN.md`; they do not establish OS resource quotas. |
| Preview/export (RM-008) | Actual PDF.js text/links/navigation/zoom, matching current-build export and annotation-free byte equality. Visible-page rendering admits up to 100 pages/25 MiB with geometry checks, per-preview canvas budgets, load/render deadlines and actual owned-worker termination. Rejected replacements retain the prior rendered PDF/position; export waits for current rendering. Stable scrollbar geometry fixes a reproduced resize loop. Final packaged viewer/lifecycle/chat and four broader workflows passed; see `PDF_VIEWER_LIMITS.md`. | Application-wide memory/CPU measurements, deeper hostile-PDF checks and physical high-DPI/VoiceOver acceptance. Canvas bounds and JS deadlines are not OS resource quotas. |
| Diagnostics (RM-009) | Source-linked errors and logs, extensionless path handling, last-good preview preserved. Recognized missing packages/classes, files, fonts, engine requirements, common syntax errors and timeouts now have specific next steps. The original output remains available; see `BUILD_HELP.md`. | Broader imported-template message coverage and Mac-version acceptance. Guided local-font selection is covered under RM-010; managed package installation remains under RM-011. |
| Templates/resources (RM-010) | Six styles covering all five specified layouts plus Modern; A4/US Letter; actual rendered thumbnails and bundled-font details; preserved origin versions; all twelve variants pass fresh-cache offline, text, link, font and geometry checks. See `TEMPLATES.md`. 516 locked resources and imported/Biber fixtures retained. Guided OTF/TTF selection, real preview and journaled source/asset apply are implemented; source-native and targeted packaged checks, plus all twelve font/template variants passed. See `LOCAL_FONTS.md`. | Apple silicon macOS-version corpus acceptance and controlled rendered-image comparisons. |
| Runtime verification/repair (RM-011) | Full-inventory verification, retained copies, Settings-only local repair, staged self-test/atomic activation and forced-interruption recovery. Signed catalog/transfer and archive/installation cores pass 26 focused tests; six service tests and the source-native Settings workflow cover reviewed import, cancel/resume, close/reload, offline retry and explicit project preview/Apply. Real offline resource/probe checks and publisher signing command are implemented. See `RUNTIME_MANAGEMENT.md` and `MANAGED_PACKS.md`. | The first public table pack, signed catalog, private renewal secret and hosted renewal are configured. Real offline corpus, HTTPS cancellation/resume and normal-app installation/preview/save/restart pass; key-retirement protocol tests preserve the rollback floor. Final packaged public-catalog acceptance and operational rotation/recovery drills remain open. Bounded historical retention, large-pack responsiveness, signing-aware manifests and power-loss/native-platform acceptance remain open. |
| Import/export (RM-012) | `.tex` and folder open, bounded validated ZIP import/export and history round-trip; durable import staging, per-file progress, forced-kill recovery, verified cleanup and interrupted-import review with outside-edit protection. Source-native recovery/import/chat checks passed; all nine exact-package native suites passed. See `ZIP_IMPORT.md`. | Apple silicon macOS-version archive compatibility, large-archive responsiveness and broader filesystem durability acceptance. |
| Usability/accessibility (RM-013) | Minimum 1040 × 680 layout, keyboard menus, shortcuts, light/dark/System, editor-size controls, chat/PDF tests; visual QA passed. | Full keyboard/screen-reader review, OS-specific high-DPI/shortcut review, reference-device performance measurements. |
| Distribution/updates (RM-014) | Unsigned Apple silicon `.app` and `.dmg`, packaged runtime smoke and full chat tests. ~289 MiB disk image / ~647 MiB app regular-file content (filesystem allocation differs). | Signed/notarized target installers, signing-aware runtime manifests, authenticated updater with recovery, stable/beta channels, relocation/upgrade/uninstall tests. Source CI and a manual Apple silicon qualification workflow are now defined; hosted acceptance is tracked in RELEASE_PIPELINE.md. No updater exists yet. |
| Release validation/notices (RM-015) | Development README, limits, tests, runtime inventory and third-party notices. Reviewable support ZIPs with native fixed-field validation, section choices, immutable export snapshots, privacy fixtures and passing source/package native tests; see `SUPPORT_BUNDLES.md`. | Complete redistribution/license materials and SBOM, Apple silicon installer checksums/provenance, measured cold/warm/p95 performance and resource budgets, completed release checklist. |
| Chat-first amendment | `CHAT_FIRST_IMPLEMENTATION.md` maps requirements to code/tests. Real Codex synthetic image check; packaged end-to-end synthetic/local-provider suite; `design-qa.md` passed. | Live signed-in Claude and real BYOK account acceptance remain unverified. These limits are disclosed; model factual accuracy is not guaranteed by visual review. |
| Documentation/community-delivery amendment | `DELIVERY_SCOPE.md` records the user-selected `grawish/folio` target and delivery evidence. Local CLI authentication is `grawish`. | Reviewed source, nine user walkthroughs, 29 screenshot demos, architecture/contribution guides, a validated documentation skill and CI/release workflows are published. Hosted source checks and automated source preview 3 passed with verified archive digests. The themed website and gallery load over HTTPS after the owner-approved domain repair. The hosted Mac baseline c861ead passed all nine native suites and artifact verification; the later 22829fd milestone passed all eleven suites, including compiler help and local fonts (`releases/mac-fonts-verification.json`). The 274ff95 guided-recovery milestone passed all thirteen suites and artifact verification (`releases/mac-save-recovery-verification.json`). Managed-pack Settings docs and screenshots now cover the development workflow; final packaged pack acceptance, complete binary third-party materials and remaining performance measurements stay open. |

## Latest concrete artifacts

- Hosted guided-recovery milestone 274ff95 passed in run 36231643343: 158 unit tests, 14 compiler integrations, all thirteen native suites, all 37 packaged-output comparisons, runtime-manifest matching and disk-image integrity. Downloaded script hashes and native evidence were checked against that exact commit. The app archive matches the independently tested/profiled recovery package. Scope and hashes are in `releases/mac-save-recovery-verification.json`; no installer was published.

- Hosted Mac milestone 22829fd passed in run 36220947706, including all eleven native suites, all 37 packaged-output comparisons, runtime-manifest matching and disk-image verification. Exact hashes and scope are preserved in `releases/mac-fonts-verification.json`. This predates support bundles and guided recovery; it does not qualify subsequent code.

- Hosted Mac baseline c861ead passed in run 36192380246, including all nine native suites, all 37 packaged-output comparisons, runtime-manifest matching and disk-image verification. Exact hashes and scope are in `releases/mac-candidate-verification.json`; this predates the compiler-help feature and its tenth suite.

- Earlier local artifacts were recorded under `release/<milestone>/`. The local `release/` directory is currently absent; its removal cause is unproven. Historical verification remains evidence of the then-tested artifacts, not a claim that those local binaries still exist. Public verification records and downloaded hosted evidence remain available.
- The current source has 198 passing unit/protocol tests (`test-results/pack-publisher-all-unit.log`), including six native-service and two explicit-pack comparison cases. Settings now supports signed imports/catalog downloads, offline retry and preview/Apply. Its source-native workflow and the existing included-compiler regression pass with zero renderer errors; exact evidence and test-publisher scope are in `MANAGED_PACKS.md`. The underlying archive/installation core passed all 15 real compiler integrations (`test-results/resource-pack-all-integration.log`), including offline resource/probe acceptance; its prior repair/startup regression also passed (`test-results/runtime-dwz1v4/result.json`). Public trust/catalog setup, the six-case table corpus, HTTPS transfer, hosted renewal and normal-app workflow now pass; final packaged acceptance remains open. See `releases/resource-pack-verification.json`. The earlier guided-save milestone passed 158 tests, including engine and store/history checks. Source-native and targeted packaged guided review tests pass; see `SAVE_RECOVERY_IMPLEMENTATION.md`. The earlier support milestone passed 135 tests (`test-results/support-all-unit.log`). Support-bundle privacy and native workflow evidence is recorded in `SUPPORT_BUNDLES.md`. Local-font corpus, source-native and persistence checks are recorded in `LOCAL_FONTS.md`. The new real compiler integration case verifies missing package/file/font/engine messages and successful local repairs (`test-results/build-help-integration-2.log`). Broader native evidence for this addition is recorded in `BUILD_HELP.md`.
- The earlier import-recovery package passed 117 unit tests and adds durable import staging, process-kill recovery and review, plus a reproduced same-project readiness fix. All nine packaged suites passed: import recovery, import, viewer, PDF lifecycle, migration, chat, repair/startup, workspace and imported-resume smoke. All recorded no renderer errors. `native-tests.json` records the exact app and script hashes; all 37 packaged outputs match, and disk-image integrity passed. Final recovery evidence is `import-recovery-zpFmni/`. These checks do not establish broader power-loss/network-filesystem guarantees, application-wide quotas or reference-device performance, or qualification of newer compiler-help code.
- An earlier startup closed unexpectedly before preparation finished. Its cause remains unproven. The failed result and logs are preserved in `test-results/compiler-migration-qualification-1790358052909/`; two subsequent full chat runs passed with added process lifecycle evidence. This is a retained reliability observation, not a claimed application fix.
- The prior milestone's 11 native compiler integration tests passed, including all twelve template variants, the original imported/Biber fixture and managed-copy corruption/repair. Its offline verification passed the full corpus. Runtime binary/resource inputs are unchanged. The current final packaged smoke separately passed actual imported/Biber compilation. Ordinary document builds retain their 30-second limit; the app-owned preparation fixture has a separate 60-second bound.
- `docs/ui-previews/chat-first-implementation/` contains stable final captures and the source/implementation comparison page.
- `docs/ui-previews/chat-first-screens/` remains the separate 24-screen visual concept set.

- Final template package tests: `test-results/templates-e3AJ8W/` (all twelve variants), `test-results/chat-LZmwWj/` and `test-results/import-aClX0f/`. All passed without renderer errors. Runtime integrity/offline checks and final picker/PDF visual inspection also passed. See `TEMPLATES.md`.

## Next independent implementation work

1. Save journaling and bounded history round-tripping are now implemented; complete target-platform durability and schema-migration interruption coverage. Guided conflict resolution is implemented and has source-native evidence. See `SAVE_RELIABILITY.md`.
2. ZIP project import, main-file selection, recoverable file operations, external-file watching and interrupted-import review are implemented. Exact-package qualification passed; keep Mac-version/large-archive checks in scope.
3. The five specified styles plus Modern, paper-size variants, actual thumbnails and offline/text/layout corpus are implemented. Guided custom-font selection now has source-native and offline corpus checks; keep full package qualification and Apple silicon macOS-version acceptance in scope.
4. Resizable panes, optional autosave, exact compiler retention, local offline repair, compiler migration and bounded PDF rendering are implemented and locally qualified. Continue signed expansion packs, compiler/OS and historical-storage limits, remaining accessibility work and Mac isolation acceptance before public release validation.
5. Complete the added user/developer documentation, feature tutorials/demos, reusable skill, noncommercial licensing, measured performance plan and publication to the new `grawish/folio` repository. Follow `DELIVERY_SCOPE.md`; a repository or screenshot alone does not prove those deliverables complete.

Signing credentials, release-hosting choices, native test environments and a signed-in Claude account may require external input later. They do not prevent the local project/template/robustness work listed above, so the goal is not currently blocked.
